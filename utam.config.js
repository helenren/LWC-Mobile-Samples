module.exports = {
    // file mask for utam page objects
    testMatch: ['HelloWord/force-app/**/__utam__/*.utam.json'],
    // output folder for generated page objects, relative to the package root
    outputDir: 'pageObjects',
};
